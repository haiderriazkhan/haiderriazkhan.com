[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/about"
  to = "/"
  status = 301

# Exact Forester note rewrites are generated in dist/_redirects during build.
# Keeping them exact prevents note routes from shadowing theme assets.
